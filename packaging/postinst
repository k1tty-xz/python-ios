#!@SHELL@
set -e

case "${1-}" in
    configure)
        printf '%s\n' \
            'Python for @SCHEME@ iOS installed successfully.' \
            'Python: @INSTALL_PREFIX@/bin/python@PYTHON_VERSION@' \
            'pip: @INSTALL_PREFIX@/bin/pip'
        ;;
esac

exit 0
